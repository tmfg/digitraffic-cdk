{
  "name": "rami",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "all-watch": "parallelshell \"npm run watch\" \"npm run bundle-watch\"",
    "build": "heft build --clean && npm run postbuild",
    "postbuild": "npm run compile-lambdas && npm run compile-canaries",
    "bundle-watch": "find src/lambda -name '*.ts' | entr -rc npx esbuild  --bundle --platform=node src/lambda/**/*.ts --outdir=dist/lambda --external:aws-sdk --target=node16",
    "cdk": "cdk",
    "ci:eslint-report": "eslint . --format html -o report.html || true",
    "clean": "rimraf dist cdk.out",
    "clean:force": "rimraf dist cdk.out node_modules",
    "compile-canaries": "esbuild src/canaries/*.ts --log-level=warning --bundle --target=node16 --platform=node --outdir=dist/nodejs/node_modules --external:aws-sdk --external:Synthetics",
    "compile-lambdas": "esbuild src/lambda/**/*.ts --log-level=warning --bundle --platform=node --format=esm --target=es2022 --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\" --outdir=dist/lambda --out-extension:.js=.mjs --external:aws-sdk",
    "link-common": "rm -rf node_modules/@digitraffic && mkdir -p node_modules/@digitraffic && ln -s ../../../../../digitraffic-common-private/ node_modules/@digitraffic/common",
    "lint": "eslint --cache .",
    "synth-sam": "cdk synth --no-staging > template.yaml",
    "test": "NODE_OPTIONS='--experimental-vm-modules' heft test --clean --max-workers 1",
    "test_errors": "npm test 2>&1 | grep 'FAIL'",
    "watch": "NODE_OPTIONS='--experimental-vm-modules' heft test --clean --watch --max-workers 1"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "~3.437.0",
    "@aws-sdk/client-secrets-manager": "~3.437.0",
    "@aws-sdk/client-sqs": "~3.437.0",
    "@digitraffic/common": "2023.9.8-1",
    "@middy/core": "^4.6.5",
    "@middy/sqs-partial-batch-failure": "^4.6.5",
    "ajv": "~8.12.0",
    "ajv-formats": "~2.1.1",
    "aws-cdk-lib": "~2.103.1",
    "constructs": "10.3.0",
    "date-fns": "~2.30.0",
    "date-fns-tz": "~2.0.0",
    "mysql2": "~3.6.2",
    "source-map-support": "^0.5.21",
    "spex": "^3.3.0",
    "zod": "3.22.4"
  },
  "devDependencies": {
    "@babel/preset-env": "~7.23.2",
    "@digitraffic-cdk/digitraffic-cdk-rig": "workspace:*",
    "@digitraffic-cdk/eslint-config": "workspace:*",
    "@digitraffic-cdk/toolchain": "workspace:*",
    "@rushstack/eslint-config": "^3.4.1",
    "@rushstack/heft": "^0.62.3",
    "@rushstack/heft-jest-plugin": "^0.10.1",
    "@types/aws-lambda": "8.10.125",
    "@types/heft-jest": "1.0.5",
    "@types/jest": "29.5.6",
    "@types/lodash": "4.14.200",
    "@types/node": "20.8.9",
    "esbuild": "0.19.5",
    "eslint": "~8.52.0",
    "lodash": "~4.17.21",
    "parallelshell": "3.0.2",
    "typescript": "~4.9.5"
  }
}
