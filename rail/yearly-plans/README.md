# yearly-plans (vuosisuunnitelmat)

This project deploys a Lambda function that fetches `yearly-plans` and `project-plans` from the source system once a day, compresses the JSON data into ZIP files and uploads them to to S3. 
