{
  "$schema": "common/autoinstallers/rush-command-line-tools/node_modules/@biomejs/biome/configuration_schema.json",
  //"$schema": "https://biomejs.dev/schemas/2.5.0/schema.json",
  "files": {
    "ignoreUnknown": true,
    "includes": [
      // In a folder with a biome.jsonc that extends this one, that path seen is one
      // starting from that folder. Therefore, these need ot be fairly generic.
      "**/*.ts",
      "**/*.tsx",
      "**/*.json",
      "**/*.jsonc",
      "**/*.html",
      // Rush managed
      "!common/scripts",
      "!common/scripts",
      "!**/node_modules",
      "!**/cdk.out",
      "!**/dist",
      "!**/build",
      "!**/output",
      "!**/temp",
      "!tmp",
      // Exclude projects lib folders
      "!aviation/**/lib",
      "!marine/**/lib",
      "!rail/**/lib",
      "!road/**/lib",
      "!other/**/lib",
      "!tools/**/lib",
      // Exclude projects sbom.json
      "!aviation/**/sbom.json",
      "!marine/**/sbom.json",
      "!rail/**/sbom.json",
      "!road/**/sbom.json",
      "!other/**/sbom.json",
      // Exclude test coverage reports
      "!**/coverage",
      // Exclude digitraffic-common subtree project
      "!lib/digitraffic-common"
    ]
  },
  "formatter": {
    "enabled": true,
    "indentStyle": "space"
  },
  "html": {
    "formatter": {
      "enabled": true
    }
  },
  "assist": {
    "enabled": true
  },
  "linter": {
    "enabled": true,
    "rules": {
      "preset": "recommended",
      "complexity": {
        "noForEach": "off",
        // Conflicts with typescript "noUncheckedIndexedAccess"
        "useLiteralKeys": "warn"
      },
      "style": {
        "useImportType": {
          "level": "error", // "error", "warn", "off"
          "options": {
            // Creates combined import for types and combined import for non types. Can make bundled lambda size smaller.
            "style": "separatedType" // "inlineType", "separatedType", "auto"
          }
        }
      }
    }
  },
  "overrides": [
    {
      // Static, hand-/tool-generated HTML templates (Bootstrap UI, Swagger docs)
      // follow framework markup conventions that trip Biome's a11y lint rules.
      // Disable those rules for HTML so formatting is still enforced without
      // rewriting third-party markup.
      "includes": ["**/*.html"],
      "linter": {
        "rules": {
          "a11y": {
            "noLabelWithoutControl": "off",
            "useAriaPropsSupportedByRole": "off",
            "noNoninteractiveElementToInteractiveRole": "off",
            "noAmbiguousAnchorText": "off"
          }
        }
      }
    },
    {
      // Allow more lenient rules in tests.
      "includes": ["**/__test__/**"],
      "linter": {
        "rules": {
          "style": {
            "noNonNullAssertion": "off"
          }
        }
      }
    },
    {
      // Allow rush configs to have comments without jsonc extension.
      "includes": [
        "rush.json",
        "common/config/rush/*.json",
        "**/rig.json",
        "**/config/*.json",
        "**/tsconfig-esm.json",
        ".vscode/*.json"
      ],
      "json": {
        "parser": {
          "allowComments": true
        }
      }
    }
  ],
  "vcs": {
    "enabled": true,
    "clientKind": "git",
    // Biome's implementation for .gitignore is not perfect,
    // and it is not working as it should with .gitignore.
    "useIgnoreFile": false,
    "defaultBranch": "master"
  }
}
